
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	МакетВыбора.Вывести(Справочники.ГосударственныеКонтракты.ПолучитьМакет("ИдентификаторыИнформацииОЗакупке"));
	
КонецПроцедуры

&НаКлиенте
Процедура МакетВыбораВыбор(Элемент, Область, СтандартнаяОбработка)
	
	Значение = МакетВыбора.Область(Область.Верх, 1).Текст;
	Попытка
		Значение = Число(Значение);
	Исключение
		Значение = 0;
	КонецПопытки;
	
	ОповеститьОВыборе(Значение);
	
КонецПроцедуры
